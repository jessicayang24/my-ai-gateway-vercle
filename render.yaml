services:
  - type: web
    name: my-ai-gateway
    env: python
    envVars:
      - key: PYTHON_VERSION
        value: "3.13.0"
      - key: LITELLM_MASTER_KEY
        sync: false
    buildCommand: pip install -r requirements.txt
    startCommand: litellm --config ./config.yaml --port $PORT
