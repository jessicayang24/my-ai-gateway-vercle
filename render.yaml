services:
  - type: web
    name: my-ai-gateway
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn litellm.proxy.proxy_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: LITELLM_MASTER_KEY
        sync: false
